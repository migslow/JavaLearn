PRÁCTICAMENTE HICE COPIA DE las 3 CLASES DE _00arraylist. DESPUÉS:

En Banco.java, 3 reemplazos de la palabra ArrayList por HashSet
Recorrido por iteración (Iterator)
Añadí a la clase Cuenta metodo equals y hashcode que vienen a continuación:

@Override
public int hashCode() {
	return (int) numero;
}

@Override
public boolean equals(Object obj) {
	Cuenta c = (Cuenta) obj;
	return nombre.equals(c.getNombre());
	
Para mostrar cuentas puedo recorrer al estilo for each
for (Cuenta c:  listaCuentas) 
	      	c.imprimeCuenta();
}