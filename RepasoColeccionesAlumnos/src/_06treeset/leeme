Utilicé la misma clase Cuenta que paquete _03ordenando
 Copié la clase Banco de _05hashset y cambié HashSet por TreeSet en clase Banco
--FIN---

Si además se quisiera cambiar el orden inicial natural (por número) a ordenación por nombre:
Copié la clase NombreComparator.java de _03ordenando
Luego añadí/cambié en el constructor de la clase Banco:
 listaCuentas = new TreeSet<Cuenta>(new NombreComparator());
