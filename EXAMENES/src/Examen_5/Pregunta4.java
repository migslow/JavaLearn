package Examen_5;

import java.io.FileReader;
import java.io.FileWriter;
import java.io.IOException;
import java.util.ArrayList;
import java.util.List;

public class Pregunta4 {
	public static void main(String[] args) throws IOException {

		// ...
		/*
		 * XStream xstream = new XStream(); xstream.alias("CATALOGO",List.class);
		 * xstream.alias("CD",CD.class);
		 * 
		 * //.... ArrayList<CD> catalogo = (ArrayList<CD>) xstream.fromXML(new
		 * FileReader("Ficheros/cdcatalogo.xml"));
		 * 
		 * 
		 * FileWriter fw = new FileWriter("Ficheros/cdcatalogo.txt"); for (CD cd :
		 * catalogo) { String linea = cd.getTitulo() + " - " + cd.getArtista() + " - " +
		 * cd.getPais() + " - " + cd.getEmpresa() + " - " + cd.getPrecio() + " - " +
		 * cd.getAnyo(); fw.write(linea + "\n"); } fw.close(); }
		 */
	}
}